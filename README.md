# RAG
Built a Retrieval-Augmented Generation (RAG) pipeline using Ollama, LangChain, Docling, Hugging Face Embeddings, and ChromaDB. The project extracts content from PDF documents, generates vector embeddings, stores them in a vector database, and enables efficient document retrieval for AI-powered question answering.
